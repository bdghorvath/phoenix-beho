[settings] 
#probe
viz = True
method = dopri5 
# make a for loop that automatically changes these settings and then create diff complete files of output/results
# make sure that output file formatting is good
# seperate training weights, and other variables in diff folders
# install weights and biases into a dictionary

# weights and biases loaded into a dictionary
# then get the weight loss and then compare everything

# below 5 can be probed
#mod
neurons_per_layer = 40 
# this could break (and this would be good)
# could use adamw but need to know weight decay better (technically better optimizer, but we'll see)
# but weight decay is 0 (W in AdamW stands for weight)
#mod
optimizer = adam 
#mod
batch_size = 17 
#mod
init_lr = 1e-3 
#mod
weight_decay = 0 

#don't touch - may need to be gpu later (for the desktop should be False)
cpu = True 

val_split = 0.082 #don't touch (for now)

# probe?
noise = 0 

#don't touch
epochs = 100 
# can modify the below 4
normalize_data = False 
batch_type = trajectory 
scale_expression = 1 
pretrained_model = False 

# TODO:
# - setting the variables via a list (for loop over the list) - picking the element of the list